[workspace]
resolver = "2"
# Stage 2 added worker/, stage 3 app/src-tauri, stage 5 mcp-fleet — last, per
# CLAUDE.md, once the control-plane surface it wraps stopped moving.
members = ["control-plane", "worker", "app/src-tauri", "mcp-fleet"]

[workspace.package]
edition = "2021"
license = "UNLICENSED"
publish = false

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
