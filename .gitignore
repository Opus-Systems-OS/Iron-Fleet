/target
/worker/work
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm
.env
.env.*
.DS_Store
__pycache__/
/target.nosync

# Claude Code per-machine settings
.claude/settings.local.json

# Buffer CLI writes a repo-local config holding the API key if run in here
.buffer/
